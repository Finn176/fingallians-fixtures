services:
  - type: web
    name: fingallians-gaa
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --timeout 60
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
